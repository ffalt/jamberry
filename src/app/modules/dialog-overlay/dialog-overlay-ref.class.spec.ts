import {DialogOverlayRef} from './dialog-overlay-ref.class';

describe('DialogOverlayRef', () => {
	it('create an instance', () => {
		const ref = new DialogOverlayRef(undefined as any);
		expect(ref).toBeTruthy();
	});
});
