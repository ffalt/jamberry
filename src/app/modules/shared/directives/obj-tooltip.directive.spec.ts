import {ObjTooltipDirective} from './obj-tooltip.directive';

describe('ObjTooltipDirective', () => {
	it('should create an instance', () => {
		const directive = new ObjTooltipDirective(undefined as any, undefined as any, undefined as any);
		expect(directive).toBeTruthy();
	});
});
